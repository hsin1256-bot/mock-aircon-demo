# 파일 경로: backend/.env.example
# 실제 배포 시 이 파일을 복사하여 .env로 저장하고 값을 채운 뒤 커밋하지 마세요.

PORT=4000

# firmware/mock_aircon_receiver/secrets.h의 DEVICE_TOKEN과 반드시 동일해야 함.
# esp32-env-power-dashboard 프로젝트의 DEVICE_TOKEN과는 무관한 별도 값이다.
DEVICE_TOKEN=CHANGE_ME_SHARED_SECRET
