# mock-aircon-demo 코드 분석 문서

> 이 문서는 코드베이스를 읽고 정리한 설명 자료입니다. 소스 자체에 있는 주석과
> README를 기반으로, 동작 원리와 설계 이유를 한 곳에 모았습니다.

---

## 1. 이 프로젝트가 하는 일 (한 줄 요약)

실제 에어컨/리모컨이 없는 시연 장소에서, **송신부(별도 프로젝트)가 쏘는 적외선(IR)
리모컨 신호를 XIAO ESP32-S3가 받아서 "이게 무슨 명령이었는지" 해석**하고, 그 결과를
웹 화면에 실시간으로 보여주는 독립된 데모 시스템입니다.

즉 실제 에어컨을 켜고 끄는 게 아니라, **"신호를 정확히 수신·판정할 수 있다"는 것 자체를
시연**하는 것이 목적입니다. 그래서 판정 결과뿐 아니라 원본 IR 타이밍 데이터(raw)까지
화면에 그대로 노출해서 "조작된 값이 아니다"라는 걸 증명하도록 설계되어 있습니다.

---

## 2. 전체 구성 요소와 데이터 흐름

```
[리모컨 또는 송신부 ESP32]
        │  38kHz IR 광신호 (특정 명령 = 특정 타이밍 패턴)
        ▼
[IR 수신 모듈] → [XIAO ESP32-S3]  ← firmware/mock_aircon_receiver
        │  캡처한 raw 신호를 irCodes.h와 비교해 판정
        │  WebSocket(ws://.../ws?token=...)
        ▼
[backend/server.js]  (Express + ws, 메모리 캐시만 사용, DB 없음)
        │  GET /api/mock-aircon (2초 폴링)
        ▼
[frontend/mock-aircon.html + mock-aircon.js]  (브라우저 화면)
```

세 계층(펌웨어 / 백엔드 / 프론트엔드)이 각각 독립적으로 실행되며, 서로 **REST와
WebSocket이라는 표준 인터페이스로만 연결**되어 있습니다.

---

## 3. XIAO ESP32-S3가 IR 신호를 "받아오는" 방식

이 부분이 질문하신 핵심이라 자세히 풀어서 설명합니다.

### 3-1. 하드웨어 단에서 일어나는 일

IR 리모컨은 사람 눈에 안 보이는 적외선을 **38kHz로 깜빡이는 빛(캐리어)**으로
쏩니다. 버튼마다 이 깜빡임을 켰다 껐다 하는 시간 간격(패턴)이 다르고, 그 패턴 자체가
곧 "명령"입니다.

- **케이스 A (권장, 이 프로젝트 기본값)**: KY-022, VS1838B, TSOP38238 같은
  "복조 IC 내장" 수신 모듈을 씁니다. 이 모듈이 내부적으로 38kHz 반송파를 걸러내고,
  **빛이 있으면 LOW, 없으면 HIGH**인 디지털 신호(active-low)를 딱 1개 핀(S)으로
  뱉어줍니다. 그래서 배선이 VCC/GND/S 3핀으로 끝납니다.
- **케이스 B (대안)**: 복조 IC가 없는 포토트랜지스터(PT334-6B 등)를 쓰면, 38kHz
  필터링·증폭·정형화를 하는 5단 디스크리트 회로를 직접 구성해야 최종적으로
  케이스 A와 동일한 active-low 디지털 신호를 얻을 수 있습니다.
- 어느 쪽이든 **`IR_RECV_PIN`(코드상 GPIO6, 보드 실크 표기 D5)에는 결국 같은 형태의
  디지털 신호**가 들어오므로, 소프트웨어 로직은 두 경우가 동일합니다
  (`mock_aircon_receiver.ino:22-31`).

### 3-2. 소프트웨어 단에서 일어나는 일 — `IRrecv` 라이브러리

```cpp
IRrecv irrecv(IR_RECV_PIN, kCaptureBufferSize, kTimeout, true);
...
irrecv.enableIRIn();   // setup()에서 한 번 호출
...
if (irrecv.decode(&results)) {  // loop()에서 매번 확인
  handleCapturedSignal();
  irrecv.resume();
}
```

- `enableIRIn()`이 내부적으로 **핀 변화 인터럽트(edge interrupt) + 하드웨어 타이머**를
  걸어둡니다. IR_RECV_PIN의 전압이 HIGH↔LOW로 바뀔 때마다 인터럽트가 발생하고,
  라이브러리는 "직전 상태가 얼마나 오래 유지됐는지"를 마이크로초 단위로 계속
  기록합니다. 이건 `loop()`와 별개로 백그라운드에서 계속 동작합니다.
- 신호가 끊긴 뒤 `kTimeout`(15µs 또는 DECODE_AC일 때 50µs) 동안 변화가 없으면
  "신호 한 개가 끝났다"고 판단하고, `irrecv.decode(&results)`가 `true`를 반환합니다.
- 이때 만들어지는 것이 바로 **raw 배열**입니다. 예: `[9024, 4512, 564, 564, 564, 1692, ...]`
  — 이 숫자들은 "빛이 켜져 있던 시간(mark)"과 "꺼져 있던 시간(space)"이 번갈아
  마이크로초 단위로 나열된 것입니다. 즉 **원본 신호 자체를 시간 배열로 통째로
  저장**해두는 방식이며, 특정 프로토콜(삼성/LG 등)로 미리 디코딩하지 않습니다.
- `kCaptureBufferSize = 1024`는 이 시간 배열을 담을 버퍼 크기, `kMinUnknownSize = 12`는
  너무 짧아서 노이즈로 볼 수 있는 신호를 걸러내는 최소 길이입니다.

### 3-3. 판정(매칭) 로직 — "어떤 명령이었는지" 알아내는 방법

이 프로젝트는 IR 프로토콜을 해석하는 게 아니라, **캡처해둔 raw 배열과 지금 받은
raw 배열이 얼마나 비슷한지를 직접 비교**하는 방식입니다.

```cpp
bool rawArraysMatch(received, receivedLen, known, knownLen) {
  if (receivedLen != knownLen) return false;
  for (i = 0; i < receivedLen; i++)
    if (!irrecv.match(received[i], known[i], MATCH_TOLERANCE_PERCENT)) return false;
  return true;
}
```

- 배열 길이가 다르면 즉시 다른 신호로 판정합니다.
- 길이가 같으면 원소를 하나씩 비교하는데, **정확히 같은 숫자일 필요는 없고**
  `MATCH_TOLERANCE_PERCENT = 25`, 즉 **±25% 오차**까지 같은 신호로 인정합니다.
  (`irrecv.match()`는 IRremoteESP8266이 자체 프로토콜 디코더에서도 쓰는 표준 공차
  비교 함수라 직접 구현하지 않고 재사용한 것.)
- 오차를 두는 이유: 실제 리모컨 신호는 매번 미세하게 타이밍이 흔들리고(지터),
  특히 케이스 B(디스크리트 복조 회로)는 노이즈가 더 낄 수 있기 때문입니다.
  주석에는 매칭 실패가 잦으면 이 값을 30~40%로 올려보라는 튜닝 힌트도 있습니다.
- 비교 대상은 `irCodes.h`에 저장된 `IR_CODE_OFF`(전원 끄기 1개)와
  `IR_TEMP_CODES`(20~24도 각각의 "냉방 ON" 신호 5개)입니다. 순서대로 다 비교해봐서
  하나라도 맞으면 `found=true`로 판정을 확정하고, 전부 안 맞으면 `UNKNOWN`입니다.

### 3-4. 온도 제어 방식이 "절대값"인 이유

일반 리모컨처럼 "1도 up/down" 델타 방식이 아니라, **온도마다 그 온도의 전체
상태 신호를 통째로 캡처**해두고 그대로 재생하는 절대값 방식입니다
(`irCodes.h:16-21`). IR은 단방향 통신이라 수신 측(에어컨)이 지금 몇 도인지
송신 측이 알 방법이 없는데, 절대값 방식이면 이전에 뭘 보냈든 상관없이 항상
목표 온도 신호를 그대로 보내면 되므로 상태 추적이나 오차 누적 문제가 없습니다.
대신 시연에 쓸 온도 개수만큼(현재 20~24도, 5개) 리모컨으로 일일이 캡처해둬야
하는 트레이드오프가 있습니다.

### 3-5. 판정 후 전송

```cpp
doc["type"]      = "mock_aircon_state";
doc["device_id"] = DEVICE_ID;
doc["matched"]   = match.found;
doc["status"]    = "ON" | "OFF" | "UNKNOWN";
doc["temp"]      = (ON일 때만) 정수 온도;
doc["raw"]       = 캡처한 raw 배열 전체;
```

판정 결과만 보내지 않고 **raw 배열을 항상 같이 실어 보냅니다.** 이는 프론트엔드가
"원본 데이터 → 해석 결과"를 나란히 보여줘서, 판정이 소프트웨어로 조작된 게
아니라 실제 수신 신호에 근거했다는 걸 시연 현장에서 바로 확인시키기 위한
설계입니다.

---

## 4. 백엔드 (`backend/server.js`)

역할은 정확히 두 가지뿐입니다.

1. **WebSocket 서버(`/ws`)** — 보드가 보내는 `mock_aircon_state` 메시지를 받아
   메모리 변수(`latestMockAircon`) 하나에만 저장합니다. **DB가 없습니다** — "이력"이
   아니라 "지금 값"만 필요하기 때문에 의도적으로 휘발성입니다. 서버를 재시작하면
   초기화됩니다.
2. **REST API(`GET /api/mock-aircon`)** — 저장해둔 그 값을 그대로 JSON으로
   반환하고, `frontend/` 폴더를 정적 파일로 서빙합니다.

### 인증 방식

- WebSocket 접속 시 `?token=DEVICE_TOKEN` 쿼리 파라미터로 인증합니다
  (`verifyClient`에서 연결 자체를 막음).
- 단순 문자열 비교(`===`) 대신 `crypto.timingSafeEqual`을 씁니다. 일반 비교는
  틀린 문자가 나오는 순간 바로 반환되기 때문에 **응답 시간 차이로 토큰을 한 글자씩
  추측하는 타이밍 공격**이 이론상 가능한데, 이를 막기 위한 것입니다. 다만 두 버퍼
  길이가 다르면 `timingSafeEqual` 자체가 예외를 던지므로, 길이가 다를 때도 더미
  비교를 한 번 수행해 실행 시간 패턴을 맞춘 뒤 `false`를 반환하도록 되어 있습니다
  (`timingSafeEqualString` 함수).
- 이 `DEVICE_TOKEN`은 원본 프로젝트(esp32-env-power-dashboard)의 토큰과는
  **완전히 무관한 별도 값**이며, 이 프로젝트의 `backend/.env`와
  `firmware/.../secrets.h`끼리만 일치하면 됩니다.

---

## 5. 프론트엔드 (`frontend/mock-aircon.html`, `js/mock-aircon.js`)

- WebSocket을 직접 쓰지 않고, **2초 간격으로 `GET /api/mock-aircon`을 폴링**하는
  단순한 방식입니다. 실시간성이 아주 엄밀하게 필요하진 않고(시연용), 폴링이 구현이
  훨씬 단순하기 때문으로 보입니다. 짧은 주기를 택한 이유는 주석에 "시연 중 즉시
  반응한다는 인상이 신뢰성에 중요하기 때문"이라고 명시돼 있습니다.
- 상태(`ON`/`OFF`/`UNKNOWN`/대기중)에 따라 배지 색상 클래스를 바꾸고, 온도와
  raw 배열, 마지막 수신 시각을 화면에 표시합니다.
- raw 배열을 화면에 그대로 노출하는 것도 백엔드/펌웨어와 같은 이유("조작되지
  않았다"는 증명)입니다.

---

## 6. 별도 프로젝트(esp32-env-power-dashboard)와의 관계 — 반드시 알아야 할 부분

이 저장소는 코드/서버가 완전히 독립적이지만, **논리적으로 딱 하나 의존하는 부분**이
있습니다.

- `firmware/mock_aircon_receiver/irCodes.h`는 원본 프로젝트의
  `firmware/esp32_sensor_node/irCodes.h`를 **손으로 복사해온 사본**입니다.
  두 저장소가 완전히 분리되어 있어서 상대경로 include로 자동 동기화가 안 됩니다.
  원본에서 리모컨을 다시 캡처해서 코드값이 바뀌면, **이 파일도 사람이 직접 다시
  복사해서 맞춰야** 판정이 정상 동작합니다. 어긋나면 항상 `UNKNOWN`만 나옵니다.
- `DEVICE_TOKEN`은 원본 프로젝트와 무관한 별도 값이라고 이미 위에서 설명했습니다.

---

## 7. 현재 상태에서 특히 짚어야 할 점 (제가 판단하기에 중요한 것들)

1. **`irCodes.h`가 전부 placeholder 상태입니다.** `IR_CODE_ON_20~24`,
   `IR_CODE_OFF` 모두 `{0}` / 길이 `0`으로, 아직 실제 리모컨을 캡처하지 않았습니다.
   지금 이 코드를 그대로 보드에 올리면 **어떤 신호를 받아도 항상 `UNKNOWN`**으로만
   나옵니다. 시연 전에 반드시 `irCodes.h` 주석에 적힌 캡처 절차(1~7단계, 15~57행)를
   거쳐 실제 값을 채워야 합니다.
2. **캡처 온도를 20~24도로 좁게 잡은 이유**가 코드 주석에 명시돼 있는데, 원본
   프로젝트의 불쾌지수 기반 추천 온도 알고리즘이 실내 습도 30~90% 구간에서 거의
   이 5도 범위 안의 값만 추천하기 때문입니다. 즉 이 범위는 "적당히 고른 값"이
   아니라 실제 추천 로직 출력 분포에 맞춘 값입니다.
3. **목표 온도가 캡처된 온도와 정확히 일치하지 않으면** (원본 프로젝트의
   `sendIRCommand()` 쪽 로직이) 가장 가까운 캡처 온도로 자동 대체해서 보냅니다.
   이 프로젝트(수신 측) 코드에는 그 로직이 없고, 어디까지나 "받은 신호를 그대로
   판정"할 뿐입니다.
4. **`IRremoteESP8266`이 해당 에어컨 브랜드를 상태 기반 클래스(`IRxxxAc`)로 이미
   지원한다면, raw capture 방식보다 그쪽이 훨씬 낫다**는 안내가 있습니다
   (캡처 불필요, 16~30도 전체 지원, 상태 추적 불필요). 지금 방식은 어디까지나
   "지원 안 되는 브랜드"를 가정한 폴백입니다. `firmware/ir_scan_tool`로 먼저
   프로토콜 지원 여부를 확인하라는 절차가 `irCodes.h` 주석에 있습니다.
5. **WebSocket은 단방향으로만 의미 있게 쓰입니다.** 보드→서버로 판정 결과를
   보내기만 하고, 서버→보드로 명령을 내릴 일이 없는 구조라 `webSocketEvent()`는
   연결/연결끊김 로그만 남기고 끝납니다(191~202행). "얘가 서버로부터 뭔가 받아서
   동작을 바꾸는 보드가 아니다"라는 걸 명확히 알아두면 구조 이해가 쉽습니다.
6. **Wi-Fi 재연결 로직**은 3초마다 상태를 확인하고, 끊긴 상태가 10초 넘게
   지속되면 재시도하는 non-blocking 패턴입니다(`delay()`로 멈추지 않고
   `millis()` 기반 타임아웃 비교로 처리) — `loop()`가 동시에 IR 수신도 계속
   폴링해야 하므로 블로킹 코드를 피한 설계입니다.
7. **데이터는 영속 저장되지 않습니다.** 백엔드가 DB 없이 메모리 변수 하나만
   쓰므로, 서버를 재시작하면 "마지막 판정 결과"도 사라지고 프론트엔드는
   다시 "대기 중" 상태로 돌아갑니다. 이력 조회나 통계가 필요하면 이 구조로는
   불가능하고, 별도로 DB를 붙여야 합니다.
8. **하드웨어 없이 서버/프론트만 검증하는 방법**이 README에 있습니다 —
   `node -e`로 가짜 WebSocket 클라이언트를 만들어 `mock_aircon_state` 메시지를
   직접 쏘는 방식으로, 보드가 없어도 백엔드~프론트 파이프라인을 테스트할 수
   있습니다.

---

## 8. 용어 미니 정리

| 용어 | 의미 |
|---|---|
| **캐리어 주파수(38kHz)** | IR LED가 빛을 켰다 껐다 반복하는 속도. 대부분의 가정용 리모컨이 38kHz를 씀. 수신 모듈이 이 주파수로 필터링해서 햇빛 등 다른 IR 노이즈를 걸러냄 |
| **active-low** | 신호가 "있을 때" 전압이 LOW로 떨어지는 방식. IR 수신 모듈 출력의 일반적인 특성 |
| **raw(mark/space) 배열** | 신호의 on/off 지속시간을 마이크로초 단위로 번갈아 기록한 배열. 프로토콜별 의미 해석 이전의 "날것" 데이터 |
| **tolerance(공차) 매칭** | 정확히 같은 숫자가 아니라 ±N% 범위 안이면 같은 신호로 보는 비교 방식 |
| **timingSafeEqual** | 문자열이 다를 때 실행 시간이 달라지지 않도록 만들어서 타이밍 공격을 막는 비교 함수 |
